spring:
  #激活配置文件
  profiles:
    active: local
  #应用名称
  application:
    name: cjyc-server-api
  #nacos Data ID：yunche-api-${spring.profiles.active}.${file-extension}
#  cloud:
#    nacos:
#      config:
#        # Nacos服务接口(不能加http前缀)
#        server-addr: 192.168.225.11:8848
#        # Group: yunche.api
#        group: yunche.api
#        # 指定配置的后缀，支持 properties、yaml、yml，默认为 properties
#        file-extension: yml
#      discovery:
#        server-addr: 192.168.225.11:8848
