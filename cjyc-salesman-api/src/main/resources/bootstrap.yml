spring:
  profiles:
    active: local
  #应用名称
  application:
    name: cjyc-salesman-api
  #nacos Data ID：yunche-api-${spring.profiles.active}.${file-extension}
  cloud:
    nacos:
#      config:
#        # Nacos服务接口(不能加http前缀)
#        server-addr: 10.253.96.126:8848
#        # Group: yunche.api
#        group: yunche.api
#        # 指定配置的后缀，支持 properties、yaml、yml，默认为 properties
#        file-extension: yml
      discovery:
        server-addr: 10.253.118.228:8848
        #server-addr: 10.253.96.126:8848 #架构测试
